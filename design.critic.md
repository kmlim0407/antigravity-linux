# Design Critique — SMOOKTH

> 마지막 업데이트: 2026-02-23

---

## 1. 브랜딩 & 아이덴티티 `70/100`

### ✅ 잘 된 점
- `BE LOGICAL ∩ BE TACTICAL` — 수학 교육 브랜드로서 컨셉이 명확하고 기억하기 쉽다.
- 교집합 기호(∩)를 네비바 로고 모티프로 재사용하는 건 **Brand Cohesion** 측면에서 수작.
- 인트로 애니메이션에서 `SMOOKTH : A → MATH WITH SMOOKTH`로 풀어내는 브랜드 스토리텔링은 차별화 포인트.

### ❌ 문제
- **배경 이미지가 해변 사진** (`unsplash photo-1507525428034`).
  수학 학원과 해변의 연관성이 없다. 이미지는 브랜드 보이스의 연장이어야 한다.
  "깨끗하고 고급스러운 느낌"을 의도한 선택이지만 브랜드 정합성을 해친다.
  → 수학적 그리드 패턴, 기하학적 텍스처, 또는 배경 제거가 더 적합.

---

## 2. 타이포그래피 `60/100`

### ❌ 비일관적인 Type Scale

현재 헤드라인 폰트 스케일 (섹션 1 기준):

| 브레이크포인트 | 크기 | 이전 대비 배율 |
|---|---|---|
| base | 20px | — |
| sm | 44px | ×2.2 (점프 과다) |
| md | 52px | ×1.18 (차이 미미) |
| lg | 72px | ×1.38 |
| xl | 90px | ×1.25 |
| 2xl | 100px | ×1.11 (차이 미미) |

**Modular Scale** 이론에서는 일관된 비율(ratio)을 따라야 한다.
황금비(×1.618), 완전 4도(×1.333), 장조 3도(×1.25) 중 하나를 선택해야 한다.
현재는 임의 숫자들이 섞여 있어 각 브레이크포인트에서 "의도적인 크기 차이"가 느껴지지 않는다.

### ❌ Letter-spacing 부족
- 현재 대문자 텍스트에 `tracking-[0.03em]` 적용.
- 대문자(All-caps)에는 `0.05em ~ 0.15em`이 업계 표준. 현재값은 거의 차이가 없다.

---

## 3. 시각적 위계 (Visual Hierarchy) `55/100`

### ❌ 섹션 2 (OT 섹션) — 요소 과다 적재

```
"OT" 120px 헤드라인
  ↓ 설명 텍스트
  ↓ 유튜브 플레이어
  ↓ 시간표 카드
  ↓ 버튼 2개
```

5개 레이어가 비슷한 시각적 무게로 쌓여 있다.
**F-패턴 / Z-패턴 스캔 이론**에 따르면 유저의 시선은 1개의 명확한 Primary CTA로 향해야 한다.
현재 이 섹션에서 유저가 무엇을 해야 하는지 불분명하다.

### ❌ "OT" 헤드라인 의미 불명확
- 120px으로 매우 크지만, 클릭도 안 되고 정보도 없는 단어.
- **Editorial Typography** 스타일은 패션/라이프스타일 브랜드에서 효과적이나,
  이 컨텍스트에서 "OT" 두 글자만 대형으로 쓰면 전달되는 정보가 없다.

---

## 4. 섹션 간 리듬 (Section Rhythm) `45/100`

현재 4개 섹션의 배경색:

| 섹션 | 배경 | 명도 |
|---|---|---|
| 1. 히어로 | 이미지 + `bg-white/80` | 밝음 |
| 2. OT | `bg-white` | 밝음 |
| 3. 플레이리스트 | `bg-white` | 밝음 |
| 4. 상담 | `bg-slate-50` | 거의 밝음 |

2→3→4 섹션이 사실상 동일한 배경이다.
스냅 스크롤을 쓰는 의미가 반감된다. 각 섹션 전환 시 "장면 전환감"이 없다.

→ 업계 표준: 밝음↔어두움 교차, 또는 색상 악센트(accent section)를 중간에 삽입.

---

## 5. 버튼 일관성 (Button Hierarchy) `50/100`

### ❌ 정의 없는 4종 버튼

| 위치 | 스타일 | 모서리 |
|---|---|---|
| 섹션 1 상담문의 | `border + bg-white` | `rounded-xl` |
| 섹션 2 IM LOGIC | `border + bg-white` | `rounded-lg` |
| 섹션 2 반별영상 | `border + bg-white` | `rounded-lg` |
| 섹션 4 상담문의 | `border-2 + bg-slate-800` filled | `rounded-xl` |

- `rounded-lg`와 `rounded-xl` 혼용.
- "상담문의"라는 동일한 액션이 섹션 1과 섹션 4에서 다른 스타일로 존재.

→ **Primary / Secondary / Ghost** 3단계 버튼 시스템 정의 후 일관 적용 필요.

---

## 6. 모션 설계 `75/100`

### ✅ 잘 된 점
- TV 켜지는 애니메이션 (`width: 4px → 90vw`) — 독창적이고 브랜드에 맞는 퍼포먼스.
- easing curve `[0.2, 0.6, 0.2, 1.02]` — 의도적 overshoot으로 생동감 있음.
- NavBar 로고 ↔ ∩ 전환의 pulsing opacity 효과 — 섬세함.

### ❌ 문제
- 아래 화살표 바운스 애니메이션이 "반별 수업영상" 버튼에만 존재.
  두 버튼이 나란히 있는데 하나만 움직이면 시선을 분산시킨다.
  의도적이라면 OK, 아니라면 제거하거나 둘 다 적용할 것.

- **인트로 애니메이션 약 6초, 스킵 없음.**
  Nielsen의 10 Usability Heuristics 중 "User control and freedom" 위반.
  재방문 유저에게 매번 6초를 강요하는 구조.
  → `localStorage`로 최초 방문 여부 판단 후 스킵 처리 권장.

---

## 7. 종합 점수

| 항목 | 점수 |
|---|---|
| 브랜딩 & 아이덴티티 | 70 |
| 타이포그래피 | 60 |
| 시각적 위계 | 55 |
| 섹션 리듬 | 45 |
| 버튼 일관성 | 50 |
| 모션 설계 | 75 |
| **평균** | **59** |

---

## 8. 우선순위별 개선 액션

### 🔴 High (즉시)
- [ ] 배경 이미지를 브랜드에 맞는 이미지/패턴으로 교체
- [ ] Primary / Secondary / Ghost 버튼 시스템 3단계 정의
- [ ] `rounded-lg` vs `rounded-xl` 통일

### 🟠 Medium (다음 스프린트)
- [ ] Modular Scale 기반 타이포그래피 스케일 재정의 (ratio 1.25 또는 1.333)
- [ ] 대문자 letter-spacing `0.08em` 이상으로 상향
- [ ] 섹션 배경 리듬 재설계 — 최소 1개 섹션을 어두운 배경으로 전환
- [ ] OT 섹션 정보 위계 단순화 — 요소 5개 → 3개 이하

### 🟡 Low (여유 있을 때)
- [ ] 인트로 애니메이션 스킵 기능 (`localStorage` 활용)
- [ ] 아래 화살표 바운스 — 제거 또는 양쪽 버튼에 동일 적용
